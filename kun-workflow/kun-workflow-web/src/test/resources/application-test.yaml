server:
  port: 18088

executor:
  env:
    name: local
    resourceQueues:
      default:
        quota:
          workerNumbers: 4
      test:
        quota:
          workerNumbers: 2

resource:
  libDirectory: /tmp

flyway:
  locations: kun-infra/
  table: flyway_kun_wf_schema_history
  baselineOnMigrate: true
  baselineVersion: 0