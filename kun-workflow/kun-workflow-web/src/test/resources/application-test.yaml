server:
  port: 18088

executor:
  env:
    name: local
    resourceQueues:
      default:
        quota:
          workerNumbers: 4
      test:
        quota:
          workerNumbers: 2
    storage:
      logDir: ${LOG_DIR:/tmp/target/logs}
      operatorDir: ${OPERATOR_DIR:/tmp/lib}
      commandDir: ${COMMAND_DIR:/tmp/lib}
      type: ${STORAGE_TYPE:LOCAL}

resource:
  libDirectory: /tmp

flyway:
  locations: kun-infra/
  table: flyway_kun_wf_schema_history
  baselineOnMigrate: true
  baselineVersion: 0